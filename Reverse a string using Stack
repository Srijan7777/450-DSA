
//return the address of the string
char* reverse(char *S, int len)
{
    //code here
stack<char>s;
for(int i=0;i<len;i++)
{
    s.push(S[i]);
}
 for(int i=0;i<len;i++)
 {
     char c=s.top();
     S[i]=c;
     s.pop();
    
    }
    return S;
}   
